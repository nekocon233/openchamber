---
title: 隧道
description: 安全地将 OpenChamber 开放给远程和移动访问。
---

# 隧道

隧道是指向你的 OpenChamber 的一个公开链接，让你可以在另一个网络上用普通浏览器访问它。使用 `openchamber tunnel` 为正在运行的实例创建一个隧道。

> 连接你**自己的设备**（移动应用、另一台桌面设备）通常不需要隧道 — 改为 [配对它们](/zh-cn/connect-devices/)，让端到端加密的 [Private Relay](/zh-cn/private-relay/) 零配置地处理外出访问。

## 前置要求

OpenChamber 会在你的机器上启动隧道提供商的 CLI。如果使用 Cloudflare 或 Ngrok，请先安装相应的提供商：

```bash
brew install cloudflared
brew install ngrok
```

Cloudflare 快速隧道可以通过 `cloudflared` 运行。Ngrok 需要 ngrok 账号，以及从 ngrok dashboard 获取的 authtoken：

```bash
ngrok config add-authtoken <your-ngrok-token>
```

FRPC 无需手动安装。OpenChamber 会自动下载并管理 FRPC 0.70.0。

## 快速开始

1. 启动 OpenChamber：

```bash
openchamber
```

如果跳过这一步，`openchamber tunnel start` 可以自动启动 CLI 服务器。自动启动时可以传入服务器选项，例如 `--port`、`--host`、`--lan`、`--ui-password` 和 `--api-only`。

2. 启动 Cloudflare 隧道：

```bash
openchamber tunnel start --provider cloudflare --mode quick
```

或启动 Ngrok 隧道：

```bash
openchamber tunnel start --provider ngrok --mode quick
```

3. 查看状态：

```bash
openchamber tunnel status
```

隧道建立后，`status` 会显示一个公开 URL。打开它，或扫描二维码，即可从任何地方访问 OpenChamber。

默认情况下，OpenChamber 会在交互式 TTY 会话中输出二维码。使用 `--qr` 强制输出二维码，或使用 `--no-qr` 将其禁用。

## 提供商

- `cloudflare`：quick、managed remote 和 managed local 模式
- `ngrok`：quick 模式
- `frpc`：通过自有 FRPS 服务器提供 TCP 或 HTTP 虚拟主机端点的 managed remote 模式

## 托管模式

### 托管远程

使用由 Cloudflare 托管的令牌 + 主机名：

```bash
openchamber tunnel start --provider cloudflare --mode managed-remote --token-file ~/.secrets/cf-token --hostname app.example.com
```

### 托管本地

使用本地的 `cloudflared` 配置：

```bash
openchamber tunnel start --provider cloudflare --mode managed-local --config ~/.cloudflared/config.yml
```

### FRPC（managed-remote）

FRPC 会将当前 OpenChamber 本地端口连接到你自建的 FRPS 服务器。两种端点都无需填写本地监听端口，OpenChamber 会自动提供。

还需要 CA 证书文件。FRPS 必须提供 SAN 与 `--frps-address` 匹配的证书；OpenChamber 会验证该身份，绝不会禁用证书验证。

#### TCP 映射

使用 `--remote-port` 创建 TCP 映射，并通过 `--public-url` 声明确切的外部 HTTPS 源地址：

```bash
openchamber tunnel start --provider frpc --mode managed-remote --frps-address 203.0.113.10 --frps-port 7000 --frps-ca-file ~/.config/frp/ca.crt --remote-port 20000 --public-url https://app.example.com:20000 --token-file ~/.secrets/frp-token
```

FRPC 会通过 TCP 映射转发明文 HTTP，因此 OpenChamber 绝不会根据 FRPS 地址推断浏览器 URL。`--public-url` 必须是 Caddy 或其他 TLS 终止器监听并转发到 FRPS 回环端口的 `https://` 源地址。系统会在签发连接令牌前拒绝 HTTP、包含凭据、路径或查询参数以及格式错误的 URL。

#### HTTP 虚拟主机

同时使用 `--custom-domain <frps-routing-host>` 和 `--hostname <external-public-host>`：

```bash
openchamber tunnel start --provider frpc --mode managed-remote --frps-address 203.0.113.10 --frps-port 7000 --frps-ca-file ~/.config/frp/ca.crt --custom-domain openchamber.internal --hostname app.example.com --token-file ~/.secrets/frp-token
```

FRPC 会生成 `type = "http"` 代理。Caddy 在公网 `443` 端口终止 HTTPS，并转发到 FRPS 的 HTTP 虚拟主机端口。它必须把 `Host` 设为 custom domain，供 FRPS 选择代理，同时把 `X-Forwarded-Host` 设为外部 hostname。例如，FRPS 的 HTTP 虚拟主机端口为 `8080` 时：

```caddyfile
app.example.com {
  reverse_proxy 127.0.0.1:8080 {
    header_up Host openchamber.internal
    header_up X-Forwarded-Host app.example.com
  }
}
```

公开 URL 的格式为 `https://<hostname>`；本例中为 `https://app.example.com`。共享 FRPS 令牌用于验证 FRPC 客户端，但不会将此 HTTP 虚拟主机与使用同一令牌的其他客户端隔离。

使用 `openchamber tunnel` 时，FRPC 令牌只能通过 `--token-file`、`--token-stdin` 或私密的交互式提示提供；`--token` 会被拒绝。通过这些方式创建的配置文件可以重复使用。

## 配置文件（managed-remote）

保存一个可重用的配置文件：

```bash
openchamber tunnel profile add --provider cloudflare --mode managed-remote --name prod-main --hostname app.example.com --token-file ~/.secrets/cf-token
```

对于 FRPC TCP，还应保存服务器和远程端口设置：

```bash
openchamber tunnel profile add --provider frpc --mode managed-remote --name frpc-tcp --frps-address 203.0.113.10 --frps-port 7000 --frps-ca-file ~/.config/frp/ca.crt --remote-port 20000 --public-url https://app.example.com:20000 --token-file ~/.secrets/frp-token
```

对于 FRPC HTTP 虚拟主机：

```bash
openchamber tunnel profile add --provider frpc --mode managed-remote --name frpc-http --frps-address 203.0.113.10 --frps-port 7000 --frps-ca-file ~/.config/frp/ca.crt --custom-domain openchamber.internal --hostname app.example.com --token-file ~/.secrets/frp-token
```

使用任一已保存的配置文件启动：

```bash
openchamber tunnel start --profile prod-main
openchamber tunnel start --profile frpc-tcp
openchamber tunnel start --profile frpc-http
```

## 直接启动环境变量

直接启动使用通用 FRPC 服务器设置和令牌。只能选择一种端点：TCP 同时使用 `OPENCHAMBER_TUNNEL_REMOTE_PORT` 和 `OPENCHAMBER_TUNNEL_PUBLIC_URL`；HTTP 虚拟主机同时使用 `OPENCHAMBER_TUNNEL_CUSTOM_DOMAIN` 和 `OPENCHAMBER_TUNNEL_HOSTNAME`。

**TCP：**

```yaml
OPENCHAMBER_TUNNEL_PROVIDER: frpc
OPENCHAMBER_TUNNEL_MODE: managed-remote
OPENCHAMBER_TUNNEL_SERVER_ADDRESS: 203.0.113.10
OPENCHAMBER_TUNNEL_SERVER_PORT: 7000
OPENCHAMBER_TUNNEL_TRUSTED_CA_FILE: /home/openchamber/.config/frp/ca.crt
OPENCHAMBER_TUNNEL_REMOTE_PORT: 20000
OPENCHAMBER_TUNNEL_PUBLIC_URL: https://app.example.com:20000
OPENCHAMBER_TUNNEL_TOKEN: <token>
```

**HTTP 虚拟主机：**

```yaml
OPENCHAMBER_TUNNEL_PROVIDER: frpc
OPENCHAMBER_TUNNEL_MODE: managed-remote
OPENCHAMBER_TUNNEL_SERVER_ADDRESS: 203.0.113.10
OPENCHAMBER_TUNNEL_SERVER_PORT: 7000
OPENCHAMBER_TUNNEL_TRUSTED_CA_FILE: /home/openchamber/.config/frp/ca.crt
OPENCHAMBER_TUNNEL_CUSTOM_DOMAIN: openchamber.internal
OPENCHAMBER_TUNNEL_HOSTNAME: app.example.com
OPENCHAMBER_TUNNEL_TOKEN: <token>
```

本地 OpenChamber 端口仍由系统自动提供。

## 实用命令

```bash
openchamber tunnel providers
openchamber tunnel ready --provider cloudflare
openchamber tunnel ready --provider ngrok
openchamber tunnel ready --provider frpc
openchamber tunnel doctor --provider cloudflare
openchamber tunnel doctor --provider ngrok
openchamber tunnel doctor --provider frpc
openchamber tunnel stop --port 3000
```

## 行为说明

- 每个 OpenChamber 实例（端口）只有一个活动隧道
- 在同一实例上启动新的模式/提供商会替换之前的隧道
- 生成新的连接链接会吊销之前未使用的链接

## 相关内容

- [连接设备](/zh-cn/connect-devices/) — 无需公开 URL 即可配对你自己的设备
- [安全](/zh-cn/security/) — 在开放访问前保护 UI
- [桌面端隧道](/zh-cn/desktop-tunnels/) — 无需从 CLI 启动即可在桌面应用中设置隧道
- [PWA 与移动访问](/zh-cn/mobile/) — 从手机访问 OpenChamber
- [问题排查](/zh-cn/troubleshooting/) — 如果隧道链接无法使用
